<div class="card-body">
  <div class="d-flex flex-start align-items-center">
    <div>
      <h6 class="fw-bold text-primary mb-1">Anonymous User</h6>
      <p class="text-muted small mb-0"><%= "Shared publicly - #{comment.created_at.strftime('%b %d, %Y')}" %></p>
    </div>
  </div>

  <p class="mt-3 mb-0"><%= comment.body %></p>
</div>